SPECIFICATION Spec

INIT Init
NEXT Next

INVARIANT TypeOK
INVARIANT Safety

\* State space reduction
SYMMETRY Permutations({dbState, fsState})

\* Temporal properties to check
PROPERTY
    \* System eventually reaches running state
    <>[](overallState = "Running")
    
    \* Checkpoint and restore operations are mutually exclusive
    [](~(checkpointInProgress /\ restoreInProgress))
    
    \* Process is stopped during restore
    [](restoreInProgress => processState = "Stopped")
    
    \* System can't be running if components aren't ready
    [](overallState = "Running" => 
        dbState = "Running" /\ fsState = "Running" /\ processState = "Running") 