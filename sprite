#!/usr/bin/env bash

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the go program in the client directory
# The subshell ensures we don't change the user's working directory
(cd "$SCRIPT_DIR/client" && go run . "$@")
